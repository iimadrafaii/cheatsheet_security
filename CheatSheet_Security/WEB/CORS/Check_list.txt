The response contains the Access-Control-Allow-Credentials

Add the header in the request : 
    Origin: https://example.com

Change the origin to an arbitrary value

Black Box :
    Does it reflect the user-supplied ACAO header ?
    Does it only validate on the start/end of a specific string ?
    Does it restrict the protocol ?
    Does it allow null origin ?
    Does it allow credentials ?


